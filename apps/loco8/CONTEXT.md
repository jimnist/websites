# CONTEXT - loco8

Ubiquitous language and durable domain facts for the **loco8.org** app. This
file is the glossary and the "why". Repo-wide architectural decisions live in
`../../docs/adr/`.

## Language

<Empty for now. Grow this via /grill-with-docs + the domain-modeling skill as
the app's domain takes shape.>

## Notes

- Uses the path alias `@/*` -> `src/*` in tsconfig.
